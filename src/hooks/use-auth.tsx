// File intentionally stubbed as part of removing the login system.
export const useAuth = () => ({ user: null, logout: () => { } });
export const AuthProvider = ({ children }: { children: React.ReactNode }) => <>{children}</>;
